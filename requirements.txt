# Runtime dependencies for Streamlit Community Cloud.
#
# Streamlit Cloud installs from this file, not from pyproject.toml, so the two
# are kept deliberately in step. pyproject.toml remains the source of truth for
# `pip install containment`; this file exists only so the hosted app builds.
#
# The app adds ./src to sys.path itself, so the package does not need to be
# pip-installed in the hosted environment.

numpy>=1.24
scipy>=1.10
shapely>=2.0
matplotlib>=3.7
PyYAML>=6.0
CoolProp>=6.5          # Span-Wagner CO2; the cubic fallback is unreliable near the critical point
pyproj>=3.5            # exact EPSG coordinate transforms for GIS export
folium>=0.15           # the GIS map on real satellite imagery
pyshp>=2.3             # ESRI shapefile export, the format agencies ask for
openpyxl>=3.1          # the Excel project workbook, in and out
streamlit>=1.30
plotly>=5.18
pandas>=2.0
